# Continuity Ledger — socsim issues #11–#16 対応

## Goal

axelrod1997 / schelling1971 構築時に判明したライブラリのフィードバック（issue
#11–#16）を後方互換で実装し，1つの PR にまとめる（前例: PR #8 "Address #1–#7"）．

**Done の定義:** 各 issue の code 部分を実装＋テスト，docs を更新，`cargo test
--workspace` グリーン，ブランチ `feat/address-issues-11-16` から PR 起票．

## Constraints

- 後方互換（既存 API は壊さない．追加が主）．
- 既存のコードスタイル（日本語コメント・テスト命名）に合わせる．
- 下流（axelrod/schelling）の改修は本 PR のスコープ外（別途）．ライブラリ側のみ．
- CI は無し．検証は `cargo test --workspace` と `cargo build`．

## Key Decisions

- 単一ブランチ `feat/address-issues-11-16` + 単一 PR（PR #8 前例に倣う）．
- #13 は「mechanism 内バッチ実行」パターンの **example + docs** で対応（フル
  イベントキュー scheduler は将来課題として note）．
- #15 の code 部分 = engine 既定 recorder を no-op（NullRecorder）にして
  socsim-log 硬依存を外す + CsvRecorder の任意列スキーマ．docs は軽量モードを主に．
- engine の lib.rs を複数 issue が触るため，grid→engine-observer→engine/log-recorder
  →docs の順で逐次実装（並行回避）．

## State

- Done:
  - [x] ライブラリ運用・テスト構成の確認（feature branch + PR，cargo test --workspace）
  - [x] ブランチ作成
- Done (実装):
  - [x] #11 socsim-grid: neighbors_into / neighbors_radius_into / neighbors_iter / Adjacency(CSR)（grid 44 tests）
  - [x] #12 socsim-grid: CellGrid<T>
  - [x] #14 engine: StepReport + step_reported + run_observed（engine 12 tests）
  - [x] #15 engine: NullRecorder 既定化（socsim-log 通常依存=0 を確認）+ socsim-core: NullRecorder + socsim-log: CsvRecorder::set_columns
  - [x] #13 docs+example: examples/cellular_automata.rs（CellGrid+Adjacency+run_observed+イベントバッチ，consensus 到達）
  - [x] #16 docs: library/usecases/architecture を EN/JA 更新，derive_seed ラベル規約（[0]=init,[1]=engine）
- Now: [→] commit / push / PR 起票（#11–#16 を close）
- Remaining:
  - [ ] PR 本文で #11–#16 を close
  - [ ] （任意・別PR）下流 axelrod/schelling を新 API（adjacency/run_observed）へ移行

## Open Questions

- UNCONFIRMED: #13 のイベントキュー scheduler を本 PR で作るか（現状: example+docs のみ）．
- UNCONFIRMED: #15 で CsvRecorder の任意列スキーマをどこまで一般化するか．

## Working Set

- リポジトリ: `/Users/akitenkrad/Documents/workspace/simulations/rs-social-simulation-tools`
- ブランチ: `feat/address-issues-11-16`
- 主対象クレート: socsim-grid / socsim-engine / socsim-core / socsim-log + docs/
- 検証: `cargo test --workspace` ; `cargo build --workspace`
- issue: #11 neighbor API / #12 CellGrid / #13 event-driven / #14 observer / #15 library-mode / #16 docs

---
*This file was generated by Claude Code.*
